## Packages
chart.js | Core requirement for dashboard charts
react-chartjs-2 | React wrapper for Chart.js
date-fns | Date formatting for bet history and charts

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

The frontend is designed with a "Dark Mode Default" sports theme. It assumes the API endpoints provided in the shared schema and routes manifest.
